export interface Feature {
    title: string;
    description: string;
    icon: string;
}

export interface MissionContent {
    title: string;
    paragraphs: string[];
}

export interface CallToAction {
    title: string;
    content: string;
    highlight: string;
}